#include <stdio.h>
#include <time.h>
#include <math.h>

int main()
{
    // int a = -3;
    // if (a < 0);
    // printf("%d is a negative number\n", a);
    // printf("%d is a negative number\n");

    // printf("this is tab\tthis is tab\n");
    // printf("this is tab     this is tab\n"); // 5 white spaces

    // int order;
    // for (int i = 0; i < 20; i++)
    // {
    //     printf("order %2d take s\n", i + 1);
    // }

    // clock_t start_t, end_t, total_t;

    // double total = 0;
    // start_t = clock();
    // printf("Starting of the program, start_t = %ld\n", start_t);

    // printf("Going to scan a big loop, start_t = %ld\n", start_t);
    // for (int i = 0; i < 100; i++)
    // {
    //     printf("%d\n", i);
    // }
    // end_t = clock();
    // printf("End of the big loop, end_t = %ld\n", end_t);

    // total = (double)(end_t - start_t) / CLOCKS_PER_SEC;
    // printf("Total time taken by CPU is %f\n", total);

    // int arr[3];
    // int d = 0;
    // for (int i = 0; i < 3; i++)
    // {
    //     printf("Enter %d\n", i + 1);
    //     scanf("%d", &arr[i]);
    // }

    // d = arr[0] * arr[1] - pow(arr[2], 2);
    // printf("d is %d\n", d);

    // int a = 1, b = 2, c = 3, d = 4;
    // if (a != 1 || b != 2 || c != 3 || d != 4)
    // {
    //     printf("I am batman\n");
    // }
    // printf("jarvis\n");

    // float a, c, b;
    // printf("Enter a ");
    // scanf("%f", &a);
    // printf("Enter c ");
    // scanf("%f", &c);
    // printf("Enter b ");
    // scanf("%f", &b);
    // if (c == 0)
    // {
    //     /* code */
    //     c = pow(a, 2);
    //     printf("c is %f\n", c);
    //     c = pow(b, 2);
    //     printf("c is %f\n", c);
    // }

    return 0;
}