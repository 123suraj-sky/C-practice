#include <stdio.h>
#include <math.h>

int main()
{
    // Method 1
    // int number;
    // printf("Enter a number: ");
    // scanf("%d", &number);
    // number = sqrt(number * number);
    // printf("It's modulus is %d\n", number);

    // Method 2
    // int number;
    // printf("Enter a number: ");
    // scanf("%d", &number);
    // if (number < 0)
    //     number *= -1;
    // printf("It's modulus is %u\n", number);

    return 0;
}